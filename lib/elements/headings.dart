part of domino.nodes.elements;

/// Creates a <h1> Element.
Element h1([content]) => new Element('h1', content);

/// Creates a <h2> Element.
Element h2([content]) => new Element('h2', content);

/// Creates a <h3> Element.
Element h3([content]) => new Element('h3', content);

/// Creates a <h4> Element.
Element h4([content]) => new Element('h4', content);

/// Creates a <h5> Element.
Element h5([content]) => new Element('h5', content);

/// Creates a <h6> Element.
Element h6([content]) => new Element('h6', content);
